# Развёртывание — v0.9

## Найдена причина «Unexpected token '<'... not valid JSON»

Это задокументированная особенность самого Apps Script, не ошибка
настройки с вашей стороны. `/exec`-ссылка на **POST**-запрос отвечает
302-редиректом на отдельный домен (`script.googleusercontent.com`), а
этот редирект либо не принимает POST вовсе, либо браузерный `fetch()`
при переходе по нему превращает POST в GET и теряет тело запроса — в
обоих случаях в ответ прилетает HTML-страница ошибки от инфраструктуры
Google, а не JSON от вашего кода. Именно поэтому у вас «всё совпало»:
проверка через прямое открытие ссылки в браузере — это GET, а он через
тот же редирект проходит нормально (тот же метод туда и обратно), в
отличие от POST.

**Исправлено радикально: все действия — и чтение, и запись (решение,
кредит, погашение, перевод, банкротство/найм, все кнопки администратора)
— теперь идут через GET с параметрами в строке запроса.** Отдельного
POST в клиентском коде больше нет вообще. `doPost` в `Code.gs` оставлен
в файле как запасной путь (например, для интеграций через
`UrlFetchApp`, который POST обрабатывает нормально), но фронтенд его
больше не использует.

Проверено тестом: все действия (`submitDecision`, `requestLoan` и
остальные) корректно срабатывают через `doGet` с параметрами-строками —
именно так их и присылает `fetch()`.

## Обновление уже развёрнутого проекта

1. Замените `Code.gs` целиком.
2. **Deploy → Manage deployments → ✏️ → Version: New version → Deploy.**
3. Замените `App.js` (изменилась только реализация `apiPost` — вызовы
   во всех остальных функциях остались прежними).
4. `tablo.js`/`tablo.html`/`tablo.css`/`index.html`/`Style.css` — без
   изменений в этой версии, менять не обязательно, но проверьте, что
   деплой на GitHub Pages актуален.

## Архитектура (без изменений)

- **`Code.gs`** — бэкенд на Apps Script, JSON API.
- **`index.html` + `Style.css` + `App.js`** — кабинет игрока/администратора.
- **`tablo.html` + `tablo.css` + `tablo.js`** — табло для проектора/ТВ.
- **`EXEC_URL` меняется в ДВУХ местах**: `App.js` и `tablo.js`.

## 1. Google Sheets + Apps Script (бэкенд)

1. Создайте таблицу → Расширения → Apps Script (или откройте существующий
   проект).
2. Скопируйте содержимое `Code.gs` в редактор, полностью заменив старое.
3. Запустите `setupSheets()`.
4. В листе **Config**: `ADMIN_USERNAME` — ваш ник без `@`; `TOTAL_ROUNDS`
   (1–36); `ROUND_DURATION_MIN`; при желании — `START_CAPITAL`,
   `EMPLOYMENT_SALARY`, `REOPEN_THRESHOLD`.
5. В листе **Players**: на игрока — только `telegram_username` (без `@`).
6. **Deploy → New deployment → Web app.** Execute as: **Me**.
   Who has access: **Anyone**. Скопируйте `.../exec`-ссылку — или, если
   деплой уже есть, используйте «Manage deployments → New version», чтобы
   ссылка не менялась.

## 2. GitHub Pages (фронтенд)

1. Публичный репозиторий, в корень: `index.html` (строго строчными!),
   `Style.css`, `App.js`, `tablo.html`, `tablo.css`, `tablo.js`.
2. `EXEC_URL` в начале `App.js` и `tablo.js` — ссылка из шага 1.6, в **оба**
   файла.
3. Settings → Pages → Source: `main` / `/ (root)`.
4. Кабинет: `https://<логин>.github.io/<репо>/` — в BotFather.
   Табло: `.../tablo.html` — на проектор/ТВ отдельной вкладкой.

## Проверка перед игрой

- Откройте `/exec` напрямую — `{"ok":true,"status":"API работает..."}`.
- В кабинете отправьте любое действие (решение месяца, кредит, перевод)
  — должно сработать без ошибки о невалидном JSON.
- Остальные проверки — как в прошлых версиях (таймер, банкротство/найм,
  переводы, индикация загрузки на кнопках).

## Технические детали API

`Code.gs` принимает **всё через GET**:

`?action=identify|dashboard|monitor|config|public|timeline|submitDecision|
requestLoan|repayLoan|transferMoney|chooseBankruptcyPath|reopenBusiness|
markBankRead|setProfile|adminUpdateConfig|adminOpenRound|
adminCalculateRound|adminResetGame&u=ник&...остальные_параметры`

`getDashboard` возвращает поле `lifecycle`: `active` / `bankrupt` /
`employed` / `left` — фронтенд ветвится по этому полю.

## Что сознательно упрощено в этой сборке (v0)

- Один сегмент рынка вместо трёх (эконом/масс/премиум).
- Переполнение ёмкости — клиенты просто теряются, без перетока к соседям.
- Формат заведения, реклама (кроме Google Карт) и вложения в качество
  действуют в том же месяце, без лага.
- `VERIFY_TELEGRAM_AUTH` выключен в `Config` — ник берётся из
  `initDataUnsafe` без криптопроверки HMAC.
- Действия теперь идут через GET-параметры, а не POST-тело — для этой
  доверенной комнаты (все свои, ник = единственный барьер входа) это не
  меняет модель угроз, но стоит иметь в виду, если игра когда-нибудь
  выйдет за эти рамки: GET-параметры видны в логах сервера и истории
  браузера заметнее, чем тело POST-запроса.
- Численные коэффициенты 6 маркетинговых каналов, зарплата в найме и
  порог переоткрытия — v0-интерпретация, требуют калибровки по живым
  прогонам.

Все цифры экономики — в листе **Config**, меняются без переразвёртывания
Apps Script (кроме структуры листов) и без прикосновения к GitHub Pages.








